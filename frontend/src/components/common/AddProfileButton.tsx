import React from "react";

const AddProfileButton: React.FC = () => {
  return (
    <button className="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-md">
      + Add New Profile
    </button>
  );
};

export default AddProfileButton;
